# Front Architecture

This file is front-owned context, not canonical registry state.

Document the confirmed semantic responsibility boundary, owned systems, and
stable implementation decisions here. The harness validates only this front's
registered root-relative path and required files; `hq-sync` does not infer or
certify semantic ownership.
