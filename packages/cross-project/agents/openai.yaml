interface:
  display_name: "Cross-Project Harness"
  short_description: "Coordinate local projects with bounded state"
  default_prompt: "Use $cross-project to orient and coordinate this local multi-project root."
